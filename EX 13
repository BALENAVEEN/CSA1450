#include <stdio.h>
#include <string.h>

char str[20];
int i = 0;

void S()
{
    if(str[i] == 'a')
    {
        i++;

        if(str[i] == 'a')
        {
            S();
        }

        if(str[i] == 'b')
            i++;
    }
}

int main()
{
    printf("Enter string: ");
    scanf("%s", str);

    S();

    if(i == strlen(str))
        printf("String Accepted\n");
    else
        printf("String Rejected\n");

    return 0;
}
